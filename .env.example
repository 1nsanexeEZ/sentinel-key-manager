POSTGRES_USER=sentinel_user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=sentinel_db
POSTGRES_HOST=postgres
REDIS_HOST=redis
SECRET_KEY=your_secret_key_here
# base64-encoded 32-byte root key (generate: python -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())")
MASTER_KEY=
# set true to unseal automatically from MASTER_KEY on startup (dev only)
AUTO_UNSEAL=false